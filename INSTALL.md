# Install Guide

1. Install Python 3.12.
2. Run `pip install -r requirements.txt`.
3. Copy `config.example.json` to `config/config.json`.
4. Fill in Discord bot token and OAuth values.
5. Run `python bot.py`.
