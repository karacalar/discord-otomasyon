# User Guide

Use `/ticket_panel` for support tickets, `/welcome_config` for onboarding, `/embed_builder` for rich messages, `/backup_create` before large server changes, and dashboard pages to manage settings and exports.
